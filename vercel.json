{
  "buildCommand": "npm run build",
  "outputDirectory": "dist",
  "installCommand": "npm install --legacy-peer-deps",
  "routes": [
    { "handle": "filesystem" },
    { "src": "/(.*)", "dest": "/index.html" }
  ],
  "crons": [
    {
      "path": "/api/cron?job=daily-reminders",
      "schedule": "0 6 * * *"
    },
    {
      "path": "/api/cron?job=expire-demo",
      "schedule": "0 0 * * *"
    }
  ]
}
